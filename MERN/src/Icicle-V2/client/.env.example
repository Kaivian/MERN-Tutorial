# .env.example - CLIENT ENVIRONMENT TEMPLATE
# Instructions:
# 1. Copy this file to .env.local
# 2. Update the values to match your local setup
# 3. Restart the Next.js server

# -----------------------------------------------------------------
# 1. API Configuration
# -----------------------------------------------------------------
# The base URL of your Backend Server.
# [SYNC CHECK]: Must match the PORT in your Backend .env (e.g., http://localhost:5000)
NEXT_PUBLIC_API_URL=http://localhost:5000

# The URL of this Frontend Application.
# [SYNC CHECK]: Must be listed in 'ALLOWED_ORIGINS' in Backend .env
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------
# 2. Authentication (JWT Sync)
# -----------------------------------------------------------------
# Identifies this frontend application.
# [SYNC CHECK]: Must match 'JWT_ACCESS_AUDIENCE_ID' in Backend .env
NEXT_PUBLIC_CLIENT_AUDIENCE_ID=icicle-client-app-fe-2025

# -----------------------------------------------------------------
# 3. Google OAuth (Public Info)
# -----------------------------------------------------------------
# The Public Client ID from Google Cloud Console.
# [SYNC CHECK]: Must match 'GOOGLE_CLIENT_ID' in Backend .env
# Value format: xxxxxxxxxx-xxxxxxxx.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-here

# -----------------------------------------------------------------
# 4. UI & Feature Flags
# -----------------------------------------------------------------
NEXT_PUBLIC_APP_NAME="APP NAME HERE"
NEXT_PUBLIC_ENABLE_REGISTRATION=true

# -----------------------------------------------------------------
# 5. Security Secrets (Server-Side Only for Middleware)
# -----------------------------------------------------------------
# [CRITICAL SYNC]: Must match 'JWT_ACCESS_SECRET' in Server .env exactly!
# Used by Middleware.ts to verify tokens.
JWT_ACCESS_SECRET=your-jwt-access-secret-here